#pragma once

#include "../Features.hpp"

namespace Cheat::AlwaysOnQoL
{
	// Always-on after inject (no menu): no-fall + casing hop (no GAS/CDO patch).
	void ApplyMovement(SDK::ASBZPlayerCharacter* pLocalPlayer);
}
